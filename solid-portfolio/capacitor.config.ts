import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lavacahacemu.portfolio',
  appName: "Luis A. Rodriguez' Portfolio",
  webDir: 'dist/portfolio/browser'
};

export default config;
